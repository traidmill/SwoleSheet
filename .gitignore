# Claude Code local memory store — not for the public repo
memory/

# clasp OAuth credentials (lives in home dir, but never commit if copied)
.clasprc.json

# Skärmskott från mobilen — arbetsmaterial för felsökning/design
skärmskott/

# Genereras av test/build-local.js ur appscript/ — ska aldrig versionshanteras,
# det var precis så den gamla kopian kunde bli fyra månader gammal obemärkt.
test/app.html
node_modules/
