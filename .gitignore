# Claude Code local memory store — not for the public repo
memory/

# clasp OAuth credentials (lives in home dir, but never commit if copied)
.clasprc.json

# Skärmskott från mobilen — arbetsmaterial för felsökning/design
skärmskott/
